/**
 * DEPRECATED: This file is kept for backward compatibility
 * Please import hooks from @/app/hooks instead:
 *   import { useAppDispatch, useAppSelector, useInitializeUser, normalizeUser } from "@/app/hooks";
 *
 * All hooks have been moved to app/hooks/ folder for better organization
 */

// Re-export from new location for backward compatibility
export {
  useAppDispatch,
  useAppSelector,
  useInitializeUser,
  normalizeUser,
} from "@/app/hooks";
