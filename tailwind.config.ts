/**
 * Tailwind Configuration - DEPRECATED
 * This file is kept for backward compatibility but is no longer used.
 * The project has been fully migrated to Material-UI (MUI).
 *
 * All styling is now handled through:
 * - lib/theme/muiTheme.ts - Centralized theme configuration
 * - MUI components - sx prop for component-level styling
 * - Emotion/styled-components - For advanced styling if needed
 */

// Empty config - Tailwind is disabled and not used
// All styling is handled through Material-UI and Emotion

export default {
  content: [],
  corePlugins: {
    preflight: false,
  },
};
